
$(function() {
    // Initialise datepickers for absence form (needs to be called twice to correctly initialize all date pickers
    initDatePickers($("#absence_date_from"), $("#absence_date_to"), 'minDate');
	initDatePickers($("#absence_date_to"), $("#absence_date_from"), 'maxDate');

    $("#holiday_year_id").change(function() {
        $("#absence_holiday_year_id").val($(this).val());
	    getAbsenceDataForGivenUrl($(this), '/absences');
    });
});